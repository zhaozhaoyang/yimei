<view class="uls">
  <view wx:for="{{info}}" wx:key="item.id" class="lis" bindtap='openguideDetail' data-cons="{{item.content}}" data-video="{{item.video}}">
    <text>{{item.title}}</text>
    <view class="flag">＞</view>
  </view>
</view>